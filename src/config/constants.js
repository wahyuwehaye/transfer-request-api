const ROLES = {
    MAKER: 'maker',
    APPROVER: 'approver',
    ADMIN: 'admin'
};

const TRANSFER_STATES = {
    PENDING: 'pending',
    APPROVED: 'approved',
    REJECTED: 'rejected',
    DONE: 'done'
};

module.exports = {
    ROLES,
    TRANSFER_STATES
};
